#include "../include/Student.hpp"

// constructor for Student Class.

// Student is a virtual class 
Student::Student(std::string name, int id, std::string major){
    name = name;
    id = id;
    major = major;
}